// all routes HTTP method (GET/PUT/POST/DELETE) will be present here

module.exports = (app) => {
    // import your existing restaurant controller here, which contains method for all CRUD operations

    // APi endpoint to create a new restaurant item

    // API endpoint to get all restaurant items

    // API endpoint to get single restaurant item

    // API endpoint to update restaurant with restaurantId

    // API endpoint to delete restaurant with restaurantId

};